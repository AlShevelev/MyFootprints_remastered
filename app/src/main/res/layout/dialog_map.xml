<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/bcg_white_top_rounds_5"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingTop="@dimen/bottomPaddingDialogPadding"
    android:paddingStart="@dimen/bottomPaddingDialogPadding"
    android:paddingEnd="@dimen/bottomPaddingDialogPadding">

    <FrameLayout
        android:id="@+id/mapContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"

        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
    />

    <ImageButton
        android:id="@+id/closeButton"

        style="@style/Button.OnPhoto"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"

        android:layout_marginEnd="@dimen/onPhotoButtonMargin"
        android:layout_marginTop="@dimen/onPhotoButtonMargin"

        app:srcCompat="@drawable/ic_close_14_14"
        tools:ignore="ContentDescription"
    />


</androidx.constraintlayout.widget.ConstraintLayout>